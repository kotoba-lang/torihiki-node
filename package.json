{
  "name": "torihiki-node",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "amu compile --target wasm32-browser worker",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "shadow-cljs": "2.28.20",
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "ws": "^8.21.3",
    "@noble/hashes": "^1.4.0"
  }
}
